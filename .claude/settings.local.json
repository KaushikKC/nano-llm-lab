{
  "permissions": {
    "allow": [
      "Bash(git config *)",
      "WebSearch",
      "Bash(python3 -c \"import torch; print\\('torch', torch.__version__, 'mps available:', torch.backends.mps.is_available\\(\\), 'cuda:', torch.cuda.is_available\\(\\)\\)\")",
      "Bash(sysctl -n machdep.cpu.brand_string)",
      "Bash(sysctl -n hw.memsize)"
    ]
  }
}
