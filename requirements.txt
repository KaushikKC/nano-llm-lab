# Core
torch>=2.12
numpy>=2.0

# Data
datasets>=5.0
# transformers 5.x pins tokenizers<=0.23.0, so keep this range compatible with it.
tokenizers>=0.22,<0.24

# Logging
tensorboard>=2.20
# wandb>=0.18  # optional, enable with --wandb

# Config / utils
PyYAML>=6.0
tqdm>=4.66

# Testing
pytest>=9.0

# Plotting (scripts/plot_loss.py, for README loss curves)
matplotlib>=3.9

# Stage 2: SFT on a pretrained Hugging Face model
transformers>=4.46
accelerate>=1.0
huggingface_hub>=0.26

# Stage 3: parameter-efficient fine-tuning (LoRA / QLoRA)
peft>=0.19
bitsandbytes>=0.49  # 4-bit quant for QLoRA; CPU-only on MPS (no CUDA kernel)
